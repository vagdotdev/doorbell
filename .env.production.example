# Copy to `.env.production` and commit after `scripts/deploy-cloud.sh`.
# Friends' installs read this — no secrets here (join phrase travels separately).
DOORBELL_BACKEND=convex
CONVEX_URL=https://YOUR-DEPLOYMENT.convex.cloud
